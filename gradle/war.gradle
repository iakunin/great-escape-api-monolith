apply plugin: "war"

bootWar {
    mainClassName = "com.greatescape.backend.GreatEscapeBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
